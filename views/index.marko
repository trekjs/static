<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">

  <title>Files within ${data.directory}</title>

  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">
  <style>
    body {
      background: #fff;
      margin: 0;
      padding: 30px;
      -webkit-font-smoothing: antialiased;
      font-family: Menlo;
    }
    @media (min-width: 992px) {
      body {
        padding: 45px;
      }
    }

    a {
      color: #1A00F2;
      text-decoration: none;
    }

    h1 {
      font-size: 18px;
      font-weight: 500;
      margin-top: 0;
      color: #000;
      font-family: -apple-system, Helvetica;
      display: flex;
    }
    h1 i {
      font-style: normal;
    }
    h1 a {
      color: inherit;
      font-weight: bold;
      border-bottom: 1px dashed transparent;
    }
    h1 a:after {
      content: '/';
    }
    h1 a:hover {
      color: #7d7d7d;
    }
    @media (min-width: 992px) {
      h1 {
        font-size: 15px;
      }
    }

    main {
      max-width: 920px;
    }

    ul {
      margin: 0;
      padding: 20px 0 0 0;
    }
    ul li {
      list-style: none;
      padding: 10px 0;
      font-size: 14px;
      display: flex;
      justify-content: space-between;
    }
    ul li i {
      color: #9B9B9B;
      font-size: 11px;
      display: block;
      font-style: normal;
    }
    @media (min-width: 768px) {
      ul li {
        width: 230px;
        padding-right: 20px;
      }
    }
    @media (min-width: 992px) {
      ul li {
        font-size: 13px;
        box-sizing: border-box;
        justify-content: flex-start;
      }
      ul li:hover i {
        opacity: 1;
      }
      ul li i {
        font-size: 10px;
        opacity: 0;
        margin-left: 10px;
        margin-top: 3px;
      }
    }
    ul a {
      color: #1A00F2;
      white-space: nowrap;
      overflow: hidden;
      display: block;
    }
    ul a:before {
      content: '\f016';
      font-family: FontAwesome;
      display: inline-block;
      margin-right: 10px;
      color: #000;
    }
    ul a:hover {
      color: #000;
    }
    ul a[class=''] + i {
      display: none;
    }
    ul a[class='']:before {
      content: '\f114';
      font-size: 14px;
    }
    @media (min-width: 768px) {
      ul {
        display: flex;
        flex-wrap: wrap;
      }
    }

    aside {
      font-style: italic;
      color: #4A4A4A;
      margin-top: 50px;
    }
    aside p {
      font-size: 12px;
      margin: 0;
      line-height: 1.5em;
    }
    @media (min-width: 992px) {
      aside {
        padding-top: 50px;
      }
    }
  </style>
</head>

<body>
  <main>
    <h1>
      <i>Index of&nbsp;</i>

      <a for(path in data.paths) href="${path.url}">${path.name}</a>
    </h1>

    <ul>
      <li for(file in data.files) >
        <a href="${file.relative}" class="${file.ext}">${file.base}</a>
        <i>${file.size}</i>
      </li>
    </ul>
  </main>

  <aside>
    <p>Node.js ${data.nodeVersion} <a href="https://github.com/trekjs/static" target="_blank" rel="noopener noreferrer">trek-static</a> running @ localhost</p>
  </aside>
</body>
</html>
